<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Calculadora</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <form action="/calcular" method="post">
      <div class="mb-3">
        <label for="numeroA" class="form-label">Número A:</label>
        <input type="text" class="form-control" name="numeroA" id="numeroA" required>
      </div>
      <div class="mb-3">
        <label for="numeroB" class="form-label">Número B:</label>
        <input type="text" class="form-control" name="numeroB" id="numeroB" required>
      </div>
      <div class="mb-3">
        <label for="operacao" class="form-label">Operação:</label>
        <select class="form-select" name="operacao" id="operacao" required>
          <option value="+" selected>+</option>
          <option value="-">-</option>
          <option value="*">*</option>
          <option value="/">/</option>
        </select>
      </div>
      <button type="submit" class="btn btn-primary">Calcular</button>
    </form>

    <% if (resultado) { %>
      <h3 class="mt-3">Resultado: <%= resultado %></h3>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
